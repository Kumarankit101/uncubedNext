// Middleware removed due to Clerk + Next.js headers compatibility issues
// Instead relying on 'use client' components and Clerk provider for auth
// Protected routes will use auth checks in components
